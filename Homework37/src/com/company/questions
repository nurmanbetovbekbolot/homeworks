1.Как вы понимаете hashCode?
Каждый объект уникален и у каждого объекта существует свой хеш-код, как у каждого человека есть свой id
Метод hashCode от Object и когда мы вызываем его, мы получаем уникальный номер объекта

2.Для чего мы можем переписать метод генерирования hashCode?

Перезаписывая hashCode мы можем управлять нашими объектами,например не позволяя их дублирования

3.Как работает HashSet?
Хеш-сет реализует Set, и позволяет хранить объекты также как массивы и листы, но в отличие от них хранит только уникальные значения

4.Какие отличия есть у HashSet & ArrayList?
В Хеш-сет Уникальность объектов, ArrayList позволяет дублирование
В Хеш-сет нет метода get, в ArrayList можно получить элемент с помощью get

5.Как вы думаете, как мы можем избавиться от дубликатов в ArrayList при помощи HashSet?
Можно из ArrayList переместить в HashSet, там дубликатов не будет, а затем обратно положить в ArrayList